<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conversion Tool</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }
        
        nav {
            background-color: #333;
            padding: 1em;
        }
        
        nav ul {
            list-style-type: none;
            padding: 0;
            display: flex;
            justify-content: space-around;
        }
        
        nav ul li {
            margin: 0 1em;
        }
        
        nav ul li a {
            color: white;
            text-decoration: none;
        }
        
        #converter {
            padding: 2em;
            background-color: white;
            margin: 2em auto;
            max-width: 400px;
            text-align: center;
        }
        
        #converter input, #converter button {
            margin: 0.5em 0;
            padding: 0.5em;
            width: 100%;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="#" id="celToFar">Celsius to Fahrenheit</a></li>
            <li><a href="#" id="lbToKg">Pound to Kilogram</a></li>
            <li><a href="#" id="ftToIn">Foot to Inches</a></li>
            <li><a href="#" id="poundToOz">Pound to Ounces</a></li>
            <li><a href="#" id="gramsToMole">Grams to Mole</a></li>
        </ul>
    </nav>
    <div id="converter">
        <h2>Converter</h2>
        <label for="inputValue">Enter Value:</label>
        <input type="number" id="inputValue">
        <button onclick="convert()">Convert</button>
        <p id="result"></p>
    </div>
    <script>
        const conversionFunctions = {
            celToFar: (cel) => (cel * 9/5) + 32,
            lbToKg: (lb) => lb * 0.453592,
            ftToIn: (ft) => ft * 12,
            poundToOz: (pound) => pound * 16,
            gramsToMole: (grams) => grams / 1000
        };

        document.querySelectorAll("nav a").forEach(a => {
            a.addEventListener("click", (event) => {
                event.preventDefault();
                const convertType = event.target.id;
                document.querySelector("button").onclick = () => {
                    const inputValue = document.querySelector("#inputValue").value;
                    const result = conversionFunctions[convertType](inputValue);
                    document.querySelector("#result").innerText = `Result: ${result}`;
                };
            });
        });

        function convert() {
            const inputValue = document.querySelector("#inputValue").value;
            // Placeholder function, real conversion logic set in event listener above.
        }
    </script>
</body>
</html>
